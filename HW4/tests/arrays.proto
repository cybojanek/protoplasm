int a[], b[][], d[], i, j;
a = new int [5];
b = new int [2][];
for(i = 0; i < 5; i++) {
    a[0] = i;
}

// 0 1 2 3 4
for(i = 0; i < 5; i++) {
    print(a[i]);
}

for(i = 0; i < 2; i++) {
    int c[];
    b[i] = new int[2];
    c = b[i];
    for(j = 0; j < 2; j++) {
        c[j] = i + j;
    }
}

// 0 1 1 2
for(i = 0; i < 2; i++) {
    int c[];
    c = b[i];
    for(j = 0; j < 2; j++) {
        print(c[j]);
    }
}

// Now write out these numbers again
// and check the next array
// to make sure we're ok with memory
for(i = 0; i < 5; i++) {
    a[0] = i;
}

// 0 1 1 2
for(i = 0; i < 2; i++) {
    int c[];
    c = b[i];
    for(j = 0; j < 2; j++) {
        print(c[j]);
    }
}

// 0 1 2 3 4
d = a;
for(i = 0; i < 5; i++) {
    print(d[i]);
}